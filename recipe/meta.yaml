{% set name = "ag-ui-langgraph" %}
{% set version = "0.0.28" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/ag_ui_langgraph-{{ version }}.tar.gz
  sha256: 663a7556182c2c24adf1a7417fad8e3c3b3721bda42ca5d8036a7b31b31cb0d2

build:
  entry_points:
    - dev = ag_ui_langgraph.dojo:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - hatchling
    - pip
  run:
    - python >={{ python_min }}
    - ag-ui-protocol >=0.1.10
    - langchain >=0.3.0
    - langchain-core >=0.3.0
    - langgraph >=0.3.25,<1.1.0
    - pydantic >=2.0.0
  run_constrained:
    - fastapi >=0.115.12,<0.116.0

test:
  imports:
    - ag_ui_langgraph
  commands:
    - pip check
  requires:
    - pip
    - fastapi >=0.115.12
    - python {{ python_min }}

about:
  summary: Implementation of the AG-UI protocol for LangGraph.
  license: 'MIT'
  license_file: LICENSE
  home: https://pypi.org/project/ag-ui-langgraph/
  dev_url: https://github.com/ag-ui-protocol/ag-ui/tree/main/integrations/langgraph/python

extra:
  recipe-maintainers:
    - rxm7706
